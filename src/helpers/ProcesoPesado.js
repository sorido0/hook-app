export const procesoPesado = ( interacionces ) => {
        
    for(let i = 0; i < interacionces; i++){
        console.log('En marcha...');
    }

    return `interaciones realizadas: ${interacionces}`;
}